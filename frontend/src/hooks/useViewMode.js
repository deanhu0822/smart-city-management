'use client';

/**
 * useViewMode — convenience re-export from DashboardContext.
 * Components that only need viewMode can import this hook directly.
 */
export { useDashboard as useViewMode } from '../context/DashboardContext';
